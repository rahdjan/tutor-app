# Скопируйте этот файл в .env и заполните реальными значениями.
# Строка подключения к PostgreSQL из Neon (Dashboard → Connect → Connection string).
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
